export const moveSpeed = 0.25;
export const rotateSpeed = 0.1;
export const gridSize = 55;
export const boundary = {
    minX: (-gridSize / 2) + 2,
    maxX: (gridSize / 2) - 2,
    minY: 0,
    maxY: 10,
    minZ: (-gridSize / 2) + 2,
    maxZ: (gridSize / 2) - 2,
};